kind: "AppHostingYaml"
version: "v1"

# Uygulamanın çalışma ayarları
runConfig:
  minInstances: 0
  # Statik bir site olduğu için baz yolunu belirtiyoruz
  basePath: "/"

# Derleme ayarları
buildConfig:
  # Vite build sonucunda oluşan klasörü hedef gösteriyoruz
  outputDirectory: "dist"
  # deneme